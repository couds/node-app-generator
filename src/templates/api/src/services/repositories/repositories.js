import MyModel from './my-domain-model';

const repositories = {
  MyModel,
};

// creating an object and exporting it makes easy to the code editor
// to autocomplete the imports
export default repositories;
